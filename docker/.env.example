# Bytebot Hawkeye - API Keys Configuration
# ===========================================
# This file contains ONLY API keys (secrets not checked into git)
# System configuration is managed in .env.defaults (checked into git)
#
# Setup Instructions:
# 1. Copy this file: cp docker/.env.example docker/.env
# 2. Add your API keys below (at least one provider required)
# 3. System configuration is already set in docker/.env.defaults
# 4. Run: ./scripts/start-stack.sh

# ===========================================
# LLM API Keys
# ===========================================
# Anthropic (Claude)
ANTHROPIC_API_KEY=

# OpenAI (GPT-4, GPT-4o)
OPENAI_API_KEY=

# Google (Gemini)
GEMINI_API_KEY=

# OpenRouter (multi-model proxy)
OPENROUTER_API_KEY=
